## 基本方針
- 不必要に他ディレクトリをスキャンしない。
- ディレクトリを指定された場合は、指定されたディレクトリ配下のみを編集対象とする。
- 変更前に必ず影響範囲を報告する。

## ディレクトリ別ルール
- 編集するファイルを含むディレクトリ直下に AGENTS.md が存在する場合は読み込む。また、より上位のディレクトリに AGENTS.md が存在しないかリポジトリルートディレクトリまで繰り返しチェックし、存在すれば読み込む。読み込んだ場合は読み込んだ AGENTS.md のパスをコンソールに出力する。
