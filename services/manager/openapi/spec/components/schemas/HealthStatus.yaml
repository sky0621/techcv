type: object
required:
  - status
  - checked_at
properties:
  status:
    type: string
    description: Health indicator value (e.g. ok)
  checked_at:
    type: string
    format: date-time
    description: Timestamp when the check was performed in UTC
